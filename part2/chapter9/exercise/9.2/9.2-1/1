情形１:r=p时,直接返回a[p]，不会出现长度为0的数组
情形２:r=p+1时,q只能等于p或r,
			   q=p时，若i=k,则返回a[p],否则对区间(p+1,r)，即(r,r),出现情形1
			   q=r时，同上
情形３:r>p+1,若q在边界上,即q=r或q=p,
				此时若i=k,则返回a[p];否则返回情形３
			 返回情形２或情形３//就不具体写了

综上:不会对长度为0的数组进行递归调用的

